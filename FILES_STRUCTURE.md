.
├── flake.nix                 (The Entry Point: Imports Host, passes "inputs")
├── hosts/
│   └── dell-xps-9380/        (The Machine: Imports Hardware + Users + Modules)
│       ├── default.nix       (The Boss: Tells the machine what it is)
│       ├── hardware-configuration.nix (Generated by NixOS)
│       └── disks.nix         (Your Disko config)
│
├── modules/                  (The "Features" library)
        
│   ├── desktop/
│   │   └── cosmic.nix        (UI logic: Desktop, Fonts, Wayland)
    └── security/
        ├── default.nix      # The "Toggle" file (imports the others)
        ├── boot.nix         # Lanzaboote / Secure Boot logic
        ├── persistence.nix  # Impermanence / Btrfs / ZFS wipe logic
        ├── secrets.nix      # Sops-nix or Age keys
        └── mitm.nix         # Hardening, Firewall, DNS over HTTPS
│   ├── system/
│   │   ├── locale.nix        (i18n, AZERTY, Timezone)
│   ├── apps/
│   │   └── default.nix       (Common apps: Git, Curl, Firefox)
│   └── virtualization/
│       └── microvm.nix       (Your MicroVM logic)
│
├── users/                    (The Identity)
│   ├── f/                    (Specific user folder)
│   │   └── default.nix       (Declares user 'f' and imports their Roles)
│   └── roles/
│       ├── dev.nix           (Role: Dev tools, Compilers)
│       └── admin.nix         (Role: Sudo, Root tools)
│
└── pkgs/                     (Optional: For custom scripts/packages)
    └── default.nix           (If you write your own nix-derivations)


hosts/
  dell-xps-9380/
    default.nix
    hardware-configuration.default.nix
    kernel-additions.nix
    disks.nix
modules/
  locale/
  desktop/
    cosmic.nix
    cosmic-greeter.nix
  security/
    secureboot.nix
  apps/
  pkgs/
  vm/
users/
  roles/
  defs/
pkgs/
flake.nix
